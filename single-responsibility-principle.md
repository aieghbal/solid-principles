# اصول SOLID - اصل اول: SRP (Single Responsibility Principle)

درود دوستان! 👋  
در این مقاله از مجموعه اصول **SOLID**، با اولین اصل یعنی **SRP** آشنا می‌شیم.

## SRP چیست؟

**SRP** مخفف **Single Responsibility Principle** است.  
ترجمه تحت‌اللفظی: **اصل تک‌مسئولیتی**

تعریف رسمی (Robert C. Martin):

> یک کلاس فقط باید به **یک دلیل** تغییر کند.

به زبان ساده:  
هر کلاس فقط باید **یک مسئولیت** داشته باشد.

یعنی:  
«یک کلاس ← یک کار ← یک دلیل برای تغییر»

## مثال نقض SRP

```javascript
class User {
  public information() {}
  public sendEmail() {}
  public orders() {}
}
```
مشکل این کد چیست؟
کلاس User همزمان مسئول این سه کار است:

مدیریت اطلاعات کاربر
ارسال ایمیل
نمایش سفارشات

→ این کلاس چند مسئولیت دارد
→ با تغییر هر کدام از این نیازها (مثلاً تغییر سیستم ایمیل یا تغییر ساختار سفارشات)، کلاس User هم باید تغییر کند
→ نقض اصل SRP
راه‌حل: تفکیک مسئولیت‌ها
```javascript
class User {
  public information() {}
}

class EmailService {
  public send(user: User) {}
}

class OrderService {
  public show(user: User) {}
}
```
حالا:

User فقط مسئول اطلاعات کاربر است
EmailService فقط مسئول ارسال ایمیل است
OrderService فقط مسئول نمایش/مدیریت سفارشات است

هر کلاس فقط یک دلیل برای تغییر دارد → رعایت SRP
مثال در سطح متد (Method Level)
کد مشکل‌دار:
```javascript
class Mailer {
  public send(text) {
    mailer = new Mail();
    mailer.login();
    mailer.send(text);
  }
}

const mail = new Mailer();
mail.send('Salut');
```
مشکل: متد send همزمان مسئول:

ایجاد شیء
لاگین کردن
ارسال ایمیل

است → چند مسئولیت دارد
نسخه اصلاح‌شده:
```javascript
class Mailer {
  private mailer;

  public constructor(mailer) {
    this.mailer = mailer;
  }

  public send(text) {
    this.mailer.send(text);
  }
}

// استفاده
const myEmail = new MyEmailService();
myEmail.login();

const mail = new Mailer(myEmail);
mail.send('Salut');
```
حالا متد send فقط یک کار می‌کند: ارسال متن با استفاده از سرویس آماده.
خلاصه

هر کلاس فقط باید یک مسئولیت داشته باشد
هر متد فقط باید یک کار انجام دهد
وقتی یک کلاس/متد بیش از یک دلیل برای تغییر دارد → احتمالاً SRP را نقض کرده است
با تفکیک مسئولیت‌ها:
کد خواناتر می‌شود
تست آسان‌تر می‌شود
تغییر دادن و گسترش دادن راحت‌تر می‌شود


در مقاله بعدی به اصل دوم SOLID (Open/Closed Principle) می‌پردازیم.
موفق باشید! 🚀
